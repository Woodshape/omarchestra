# Local Workbench v1 — first useful Assignment loop tracker

Status: **ACTIVE; review-fix follow-up.** `cad005e` checkpoints AL-01–AL-07 unchanged. Independent review found eight execution/composition defects despite the passing slice gates. The [review-fix checkpoint](../reviews/assignment-loop-review-fixes.md) records their corrections, native Candidate-tool composition, failure regressions and validation. These are bounded engineering results, not independent re-review or full live-intervention acceptance. No real Pi dispatch, installation, reload, or restart was performed. AL-08 remains **BLOCKED** pending its separately authorized live-use prerequisites.

This is the current progress tracker for the one-Pi, one-Goal, one-Assignment slice in the [main Local Workbench plan](local-workbench-v1.md). Product and safety authority remains the locked [MVP](../design/mvp.md), approved [agent-coordination direction](../design/agent-coordination.md), and [Local Workbench contract](../design/local-workbench-v1-contract.md), especially C2 and C5–C14. Transition/failure semantics are in [the transition tables](../design/local-workbench-v1-transitions.md). This tracker schedules implementation; it does not weaken or replace those contracts.

The operator has since reported review complete and authorized closing the live prerequisites. The [live-readiness checkpoints](../reviews/assignment-loop-live-readiness.md) record the read-only preflight and explicit offline schema-upgrade engineering gate. Native intervention and the reviewed live setup/task remain pending; no installed root has been migrated or activated.

## Finish line

For one local Execution Node and Project, the operator can:

1. Select one Goal, one exact ready visible Pi Run, one configured acceptance gate, a bounded task, and stop/correction limits.
2. Review and confirm the exact current Project/Git baseline, Goal, Run/connection/control identity, frozen check definition/resources, task text, and limits.
3. Admit exactly one write-authorized Assignment for that Project in a durable transaction, then deliver one stable-ID message to that same Pi.
4. Receive an explicit, bounded Candidate from that exact Run/Attempt and associate it with the frozen gate and checkout evidence.
5. Run the configured gate under the accepted time, output, environment, and resource bounds. Record a truthful pass/failure/attention result; a pass completes the one-Assignment Goal only after final revalidation.
6. Recover from disconnects, duplicate frames, restart, timeout, takeover, stop, and lost acknowledgements without inventing success, silently releasing an uncertain writer, or blindly resending work.

The context digest is only a fresh same-process Project-root report. It is not the Git HEAD/baseline fingerprint, a filesystem snapshot, admission, writer authority, or proof of Pi API delivery.

## Scope and invariants

- One local Node, one Project, one Goal, one ready visible Pi, one Assignment and one configured gate. At most one active/uncertain writer per Project.
- Keep one Owner/Runner authority and its existing store/receipt transaction path. No parallel registry or prototype promotion.
- Navigation and the dock remain presentation-only. QML never computes admission, Git facts, gate results, or writer authority.
- Commit authority and delivery intent before sending. Delivery is not exactly-once; an uncertain outcome is not an automatic retry.
- Only a structured Candidate from the exact current Run/Attempt/control epoch can enter validation. Agent-settled, conversational “done,” and artifact appearance are not acceptance.
- A configured gate is the acceptance authority; label only what that gate proves. No mandatory Reviewer, multi-agent workflow, Message Board, delegation, remote execution, CLI/API, worktree isolation, Pi/process kill, or rollback in this slice.
- No install, shell reload, Owner restart, or Pi reload is implied by implementation or by a passing automated gate. Each requires separate authorization. Live dispatch waits for the intervention prerequisites below and a separate explicit operator authorization.
- Do not require another Adoption walkthrough/microtest as a prerequisite. Use framed fake-Pi and disposable-store integration evidence for this implementation.

## Status legend

- **DONE** — code and its stated executable gate pass; not a claim of live acceptance.
- **IN PROGRESS** — bounded implementation currently underway.
- **NOT STARTED** — no implementation slice has landed.
- **BLOCKED** — intentionally cannot proceed until the listed prerequisite/authorization is met.
- **OPEN TOPIC** — an implementation/evidence gap under an already selected contract. It is not permission to change product policy; bring any contract conflict back for explicit decision.

## Progress

| ID | Slice | Status | Evidence / exit condition |
| --- | --- | --- | --- |
| AL-00 | Durable Project/Goal/check/Adoption substrate and actual Owner/Companion composition | **DONE (engineering)** | Current canonical [Phase 2 integration result](../design/local-workbench-v1-phase-2-integration-result.md) and `just --no-dotenv local-workbench-v1-phase-2-check`. Engineering gate passes; live Project/Goal/Adoption use remains separately unperformed. Do not repeat Adoption tests to start this plan. |
| AL-01 | Fresh, challenged Project-root context prerequisite and fail-closed Start Review presentation | **DONE** | Commit `50156de`; `just --no-dotenv local-workbench-v1-phase-3-context-check`; covers no unchallenged register attestation, capability negotiation, match/mismatch/missing/stale/disconnected reports, raw-path exclusion, and rendered UI. AL-01 alone does not enable Start; the composed AL-07 gate does. |
| AL-02 | Exact start proposal, baseline/check revalidation, durable Assignment/Attempt admission, single-writer fence and assignment outbox | **DONE (engineering; no live dispatch)** | Schema 10 single-store lifecycle substrate with typed methods. The transient `prepare_start_review` proposal and `start_assignment` confirmation revalidate the C9 baseline/check/context; only the presentation route may confirm Start, while direct `handleIntent` rejects it. Admission commits Assignment, Attempt, held Project writer epoch, event/revision, complete intent receipt and queued outbox in one transaction. Exact-envelope replay returns the retained receipt; changed reuse, stale/tampered/drifted proposals, held/uncertain writers and concurrent starts are refused with no partial authority. Fault injection at every durable boundary and reopen recovery (queued → `not_sent`/`owner_restarted`, held writer → uncertain; never sent or released) are covered. Gate: `just --no-dotenv local-workbench-v1-phase-3-admission-check`. The composed AL-07 gate now enables Start only for an eligible current review; this is not authorization for live dispatch. |
| AL-03 | Same-Pi committed delivery, bounded ACK, extension dedup and uncertainty reconciliation | **DONE (engineering; no live dispatch)** | `AssignmentDeliveryCoordinator` (`runner/bridge-delivery.ts`) consumes the single queued outbox row, write-ahead marks `attempting` before the one wire send, and settles exactly one bounded ACK. The surviving extension (`runner/pi-bridge-extension.ts`) retains delivery evidence keyed by `(assignmentId, attemptId, deliveryId, payloadDigest)` before ACK: exact replay is `duplicate` with the stored outcome, conflicting reuse is `invalid`/`delivery_conflict`, `busy` does not queue, and a missing Pi API refuses before invocation. A thrown or asynchronous send retains a write-ahead `unknown` receipt, never not-sent; duplicates and receipt queries cannot repeat that invocation. Lost ACK and post-send transport failure leave the outbox `unknown`/`transport_error` and the writer uncertain; only the explicit receipt query (`assignment_receipt_request`/`assignment_receipt`) resolves it, never an automatic resend. Extension restart/reload inherits no dedup identity. Gate: `just --no-dotenv local-workbench-v1-phase-3-delivery-check`. Real Pi delivery remains untested and blocked under AL-08. |
| AL-04 | Structured, bounded Candidate and artifact association | **DONE (engineering)** | The registered native Pi tool `omarchestra_submit_candidate` derives exact identity from the received Assignment and uses the dedicated structured port (`runner/candidate-submission.ts` + `candidate_submission`/`candidate_receipt` frames) carries only the exact bounded C6 payload from the current committed connection. `validateCandidateSubmission` (`runner/candidate.ts`) proves the exact field set, identity, summary and path bounds, then the coordinator re-verifies every artifact on disk (regular non-symlink file inside the confirmed Project root, declared length, stable SHA-256). `store.submitCandidate` fences assignment/run/control-epoch/binding-incarnation before any write, derives `preManifestDigest` from the frozen admitted manifest, and makes an exact duplicate idempotent while changed reuse, drifted epoch and retired/purged identity reject before mutation. A lost receipt leaves the durable association intact and reports `unknown`. Gate: `just --no-dotenv local-workbench-v1-phase-3-candidate-check`. Live Candidate submission remains untested and blocked under AL-08. |
| AL-05 | Frozen gate invocation, bounded executor, checkout/resource fingerprinting and accepted result | **DONE (engineering; no live dispatch)** | `WorkbenchAuthority.executeAssignmentGate` (a direct-call async port, never routed from `handleIntent`) revalidates the frozen check digest/executable, the exact held writer epoch, Run control epoch and absence of stop, then captures a C9 baseline, re-verifies the Candidate's artifacts on disk, and runs the one bounded no-shell `executeGate` child. Pre/post double-scans compare the full Git/context baseline and worktree manifest. A final acceptance scan, fresh exact challenged quiescence query, current gate version and rehashed resource/executable pins feed one `store.resolveGateAcceptance` transaction that rechecks candidate/gate/writer/epoch/stop/quiescence and completes the Goal only for a genuine pass; bounded C13 evidence excludes raw child output and full paths. `just --no-dotenv local-workbench-v1-phase-3-assignment-gate-check` covers clean pass, default-quiescence fail-closed, nonzero, spawn error, timeout, output cap, candidate/resource mutation, an unterminated child, and stop/control-epoch races; every non-pass and unknown outcome stays nonaccepting. The AL-07 composed gate also exercises one deterministic fake validator through the presentation route. |
| AL-06 | Assignment-specific stop, takeover, bounded correction, writer uncertainty and recovery | **DONE (engineering; no live dispatch)** | Explicit direct-call `WorkbenchAuthority` ports (never routed from `handleIntent`): `stopAssignment` durably revokes queued dispatch first (`queued→not_sent('revoked')`), records the unique stop, closes the Assignment/Attempt, and either retains the writer as `uncertain` when effects were possible or releases it when none could have been sent; it never claims Pi/tool termination and cancellation stays `not_requested`. `takeAssignmentControl`, the actual dock Take control route and framed interactive input share the Assignment pause mutation in the binding/epoch transaction: `manual_takeover`, live Attempt/Assignment `attention` and revoked queued delivery, while keeping the held writer; `store.submitCandidate` now also fences the binding control epoch so a late Candidate never writes and `store.resolveGateAcceptance` refuses a late pass with `control_epoch_changed`. `recordAssignmentHandoff` persists one exact structured handoff (canonical `handoffDigest`) and enters `reconciling`; `reconcileAssignment` requires the current projection revision, the exact held writer, a `ready` binding, no stop and remaining `maxCorrections`/`elapsedMs`, then creates the next frozen-gate Attempt (`ordinal+1`, `writerEpoch+1`, queued delivery) or auto-stops on `attempt_limit`/`elapsed_limit`. `RecoveryReport.reconciliationRequired` surfaces active Assignments whose writer became uncertain on restart, so an unprovable interval blocks continuation without mutating lifecycle state. Admission additionally persists an owner-epoch monotonic elapsed budget; delivery, pre-launch, active-gate deadline, acceptance and native hidden-dock ticks enforce it without wall-clock resets. Coverage: `just --no-dotenv local-workbench-v1-phase-3-intervention-check` plus the review-fix delivery/gate/native tests, including takeover/stop fencing and restart recovery. Start is enabled only through the current transient presentation review after AL-07; real dispatch remains blocked under AL-08. |
| AL-07 | Real UI → Owner/Runner → SQLite → challenged fake Pi → Candidate → validator composition and executable acceptance gate | **DONE (engineering; fake-only)** | `just --no-dotenv local-workbench-v1-phase-3-assignment-loop-check` passes the real QML/adapter → Runner → SQLite → framed fake-Pi → registered native Candidate tool → `/usr/bin/true` gate path and authoritative SQLite reopen. It covers stale confirmation, injected admission rollback with no send/receipt, exact Start replay without a second send, invalid/old Candidates, and QML Accept. AL-02/05/06 gates cover remaining transaction boundaries, mutation/timeout, and takeover/stop races. No live dispatch is authorized. |
| AL-08 | One separately authorized real local task | **BLOCKED** | Requires AL-02–AL-07, the live-dispatch prerequisites below, a reviewed release/setup plan, and explicit authorization for each needed installation/reload/task action. No authorization is granted by this plan. |

## Implementation slices and gates

### AL-02 — Admission and exact confirmation

Implement only the minimal one-Assignment domain needed by C2, C5, C7, C9 and C10. Keep preparation/review separate from admission. The Runner—not QML—must resolve and freeze:

- Project ID, Execution Node, canonical root, Git common-directory identity and Project revision;
- current HEAD/index and the bounded dirty/untracked/ignored baseline digest;
- Goal ID and exact target Run plus immutable Pi incarnation, current challenged connection, readiness and control epoch;
- check ID/version/digest, canonical definition and executable/resource pins;
- exact task text, correction/elapsed limits, and resulting Assignment/Attempt/delivery IDs.

At confirmation, revalidate current facts rather than trusting the proposal or `contextMatch`. Any Project/HEAD/baseline/check/resource/Goal/Run/connection/control/revision change invalidates the review and requires a fresh one. In one Runner transaction, persist the Assignment, Attempt, single-writer admission/epoch, event/revision, complete intent receipt and assignment delivery outbox. A second concurrent start, another Role on the same Project, or any held/uncertain writer must be rejected. Receipt/transaction failure must leave no writer or sendable outbox item. Start is now available only through the transient review and presentation confirmation route after AL-07; it is still not authorization for a real Pi send.

#### AL-02a — Bounded baseline capture and durable admission (complete)

`runner/content-manifest.ts` now captures the registered Project/Git identity, current HEAD/status, raw HEAD/index/config state, effective local config (including includes) and a sorted content/mode manifest of the working tree. It includes ignored/untracked files, records symlink text without following targets, rejects special/nested Git storage, bounds paths/files/bytes and requires two matching complete scans within 60 seconds. The disposable test covers content/mode/HEAD/index/config changes, included-config drift, mutation between scans, identity drift, oversized/special files and deadline failure. Start Review captures and confirmation revalidates this baseline; admission persists its digest with the Assignment/Attempt/writer/event/receipt/outbox in one transaction. The baseline digest is not a live dispatch grant. `STORE_SCHEMA_VERSION` is 10 with a forward-only Assignment/Attempt/writer/outbox substrate; schema-9 stores are refused byte-unmodified by startup. A separate [plan-bound offline upgrade](../reviews/assignment-loop-live-readiness.md) now adds the schema-10 substrate while preserving history; there is still no silent migration.

### AL-03 — Delivery and honest uncertainty

Implemented on the existing challenged bridge through `pi.sendUserMessage`. `AssignmentDeliveryCoordinator` re-checks the current binding, connection, challenge, writer epoch and deadline through `BridgeRegistry.prepareAssignmentDelivery` before the single send. Delivery frames are the exact canonical admitted Assignment frame (`payloadJson` with `payloadDigest = sha256(payloadJson)`), capped at 64 KiB. ACK/receipt callbacks bind through `setAssignmentHandlers` so adoption and delivery coexist. The durable outbox is forward-only: `queued → attempting → written|unknown|not_sent`, `unknown → written|not_sent`; there is no `→ queued` edge and no blind resend. Fake-only faults are covered in `test/phase-3-assignment-delivery.test.ts`; the installed Pi message API is not evidence of exactly-once delivery.

Use the same current challenged Pi bridge and same visible Pi process. The send occurs only after the admission transaction commits and a final currentness check. Stable identity is `(assignmentId, attemptId, deliveryId, payloadDigest)`; the surviving extension records/deduplicates it and reports `accepted`, `busy`, `duplicate`, `invalid`, or explicit `unknown` after an unproven Pi API invocation. `accepted` proves only that the Pi API accepted input, not completion. `busy` does not queue. Commit-before-send failures, accepted-then-throw, connection replacement, duplicate/conflicting frames, ACK loss, Owner restart, and receipt-query recovery must be injected. No recovery path creates a second turn automatically.

### AL-04 — Candidate

Add a dedicated bounded extension-to-Runner submission, not a general agent message. Validate the exact current Run/Attempt/control epoch before persistence. Use C6 bounds: one pending Candidate per Attempt, bounded summary and artifact reference count/path lengths, relative Project-contained paths, no symlink traversal, stable content digests and no transcript/tool output. Exact duplicate submission is idempotent; changed reuse rejects. Candidate receipt is not gate success.

### AL-05 — Gate and acceptance

Reuse the Project-scoped resolved check catalogue; do not treat configuration-time hashes as execution proof. Freeze the exact check definition/resources per Attempt. Follow C7–C9: explicitly declared resource closure, absolute executable/argv, no shell, constructed nonsecret environment, bounded timeout/output, one bounded Runner-owned child and scratch area, and no Pi/process-group kill. Double-scan the candidate before launch and after exit; a changed checkout or validator resource is nonaccepting even on exit 0. A gate pass remains provisional until the final transaction rechecks Candidate, gate, context, writer/control epochs, stop/takeover state and quiescence. Store bounded reason/evidence and keep general projection free of validator output.

Test normal pass, nonzero, spawn error, timeout, output cap, changed executable/resource, candidate mutation, ignored/untracked changes, path escape/symlink/special file, scan bounds, and simultaneous takeover/stop/epoch changes. Every non-pass and unknown child/quiescence outcome stays nonaccepting and preserves uncertainty.

### AL-06 — Limits and intervention

Persist correction count and elapsed accounting; a restart with an unprovable interval does not grant a fresh budget. Failure may produce only the bounded next Attempt under the same confirmed gate and remaining limits. Gate execution error, changed evidence, unknown delivery, timeout, or uncertain quiescence requires attention/reconciliation, not an automatic correction loop.

Takeover and interactive-source input pause future Assignment/message delivery and invalidate stale Candidate/pass acceptance through control-epoch fencing. Return-to-team requires an exact structured handoff and explicit reconciliation. Stop durably revokes new dispatch first; cancellation is a separate, tested capability and ACK, never proof Pi tools ended. No kill, terminal close, checkout rollback, or automatic release. A disconnected/retired/replaced Run does not clear the writer.

## Open implementation topics

The fake-only engineering path is composed and gated. These remaining items limit real Pi use or require stronger live evidence; they do not authorize work beyond AL-07:

| Topic | Selected rule | What still needs proof | Effect if unavailable |
| --- | --- | --- | --- |
| Pi send/candidate API | C5 uses same-process `pi.sendUserMessage()`; C6 uses a dedicated structured Candidate frame. | Fake extension/bridge tests cover the adapter and our durable receipt protocol. Installed Pi docs/examples do not prove the API's exact delivery/receipt contract; no Pi API is evidence of exactly-once delivery. | AL-08 stays blocked until live semantics and risk are explicitly reviewed; never blind-resend. |
| Admission baseline | Dirty checkout is allowed only against an explicitly confirmed baseline; C2/C9 define separate directory identity, Git facts and content manifest. | Start Review captures and confirmation revalidates the baseline; admission persists its digest. The composed gate covers clean-checkout fake execution; AL-02/05 suites cover baseline and mutation windows. | Any drift rejects admission or acceptance and requires fresh review. |
| Durable schema evolution | Assignment/outbox state must share the existing Runner's single authority and transaction semantics. | Store is schema 10; startup refuses schema 9 unchanged. The separately authorized U1 offline-upgrade slice proves exact schema-9 validation, backed-up in-place transaction, history/fence preservation and failure windows in disposable roots. Live application is pending. | No silent migration or repair; any compatibility change needs its own reviewed plan. |
| Gate resource closure | The configured executable/resources are operator-declared and versioned; undeclared dynamic dependencies are not reproducible evidence. | Prove pre/post hashes and execution behavior for declared resources; copy into Runner-owned storage only where semantics permit, otherwise pin and recheck checkout resources. | Check unavailable; no gate launch or acceptance. |
| Quiescence and activity coverage | C9/C11 require fresh challenged evidence plus operator reconciliation where coverage was lost; `ctx.isIdle()` is best-effort and is not OS proof. | Fake AL-05/06 tests cover fail-closed outcomes, source takeover, lost coverage and restart. They do not prove live Pi or OS process state. Preserve the accepted slash/user-bash limitation; do not read content to improve detection. | Retain uncertain writer and block conflicting work. |
| Cancellation and descendants | C8/C12 allow only tested cooperative cancellation; no kill escalation. | Advertise/use only a supported Pi API; bound validator child lifetime/output and prove cleanup or quarantine on uncertain descendants. | Mark unsupported/unknown, revoke future dispatch and retain uncertainty. |
| Privacy of results | C13 permits explicit task/Candidate content under bounds; general activity excludes gate stdout/stderr and conversation data. | Test every persisted/projected field and diagnostic truncation/redaction boundary. | Suppress restricted detail; never make it a general projection. |
| Existing owner-data compatibility | No implicit state deletion or silent schema repair; the live owner/release lifecycle is separate from code tests. | U1's explicit receipt/schema upgrade composition and failure gate pass on disposable fixtures. The actual installed v2 receipt/schema-9 root remains unchanged; apply only after the remaining live-use prerequisites and exact plan review. | Do not install/restart against real state. |

If implementation evidence conflicts with a locked C2/C5–C14 decision, stop at that boundary and record the exact evidence/request a decision; do not weaken the contract to make the gate green.

## Acceptance and live-use gates

The fake-only AL-07 gate now composes the real QML/adapter, Owner/Runner authority, SQLite transaction, framed fake Pi, structured Candidate submission, and `/usr/bin/true`. It passed as `just --no-dotenv local-workbench-v1-phase-3-assignment-loop-check`. The composed test covers stale confirmation, one injected admission rollback with no receipt/send, replay without a second send, wrong Run/control epoch Candidates, QML Accept, and authoritative reopen. The separate AL-02–AL-06 gates cover all admission fault boundaries, delivery ACK/receipt loss, Candidate association, gate mutation/timeout/output/spawn failures, and stop/takeover/restart races. These are engineering fakes, not evidence of live Pi exactly-once delivery or OS quiescence.

Before any real Pi/worktree dispatch, prove the Phase 4 prerequisites in D6 of the [bounded handoff](local-workbench-v1-phase-3-handoff.md): stop authority/blast radius, writer quiescence and reconciliation, takeover during delivery, unknown-outcome recovery, diagnostics/privacy, and retirement/replacement/purge under live-load semantics. This is an engineering safety gate, not a request to retest Adoption.

A real task is a separate operation. Obtain explicit authorization before any installation/setup, shell reload, Owner restart, Pi reload, or real Assignment/gate execution. Record the task scope, exact check, observed outcomes, uncertainty and skipped steps. Do not push without authorization.

## Updating this tracker

After each bounded slice, update its status, commit/evidence, exit-gate outcome and newly discovered open topics here. Keep one checkout writer. Preserve verified partial work and turn failures into regression tests. Do not mark a slice DONE because helper/fixture tests pass if its stated composed path is still missing.
