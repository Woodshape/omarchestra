# 2026-09-25 — reboot-time owner receipt failure and bounded recovery

Status: **The original v1→v2 `/run` recovery passed, but a later reboot exposed a second persistent `st_dev` drift. The v2→v3 stable-filesystem-identity correction and failed-unit launcher regression pass targeted disposable tests. Exact live migration is pending operator confirmation; no persistent receipt or service state has been changed in this follow-up.**

## Failure evidence (read-only)

At graphical login, `omarchestra-workbench-owner.service` was `enabled` but failed with `identity_drift` on three start attempts and reached `start-limit-hit`. `~/.local/state/omarchestra/workbench-phase2-live/ownership.json` is a historical v1 receipt: the persistent state tree, SQLite lock/store/fence inodes and their parent chain still match it. Only `/run/user` (old inode 3580 → current 3584) and `/run/user/1000/omarchestra-workbench-phase2-live` (old 1447 → current 169) changed. The new runtime root is owner-only mode 0700 and empty; the service lock is released. This is the expected recreation of ephemeral `/run`, not evidence that the durable Project and Goal were replaced. No original socket or active Pi process survives reboot.

`pi list` still shows the global `~/.local/share/omarchestra/pi-observer.ts` entry; its file remains mode 0600. With no Pi process and no listening owner, there is no observed session or footer. A new visible Pi launched normally after owner recovery should load the observer; do not synthesize or adopt one for this check.

## Corrected contract and disposable evidence

At the original checkpoint, `runner/owned-resources.ts` recorded v2 receipts and an explicit v1→v2 migration repaired `/run` inode pinning. That preserved same-owner device/inode checks but still persisted `st_dev`, which proved unstable for this Btrfs-backed `/home` across a later restart. The follow-up implementation writes v3 receipts pinning the mount source/root/type/mountpoint tuple, Btrfs subvolume ID when applicable, inode, owner and kind. Each live Owner lifetime pins and rechecks mount ID plus inode/owner/type only; it never pins `st_dev`. Startup does not rewrite old receipts. An explicit v1/v2→v3 plan validates the old receipt, reports paths where its obsolete `dev` field differs without carrying device numbers into the plan or new receipt, requires the private empty runtime root and exclusive owner lock, preserves old receipt bytes outside the state root, and performs an atomic forward-only write. Interruption leaves evidence for manual recovery.

Fake filesystem/SQLite tests cover reboot recreation twice, same-lifetime replacement, retained meta/node identity, durable lock substitution, second-owner refusal, nonempty runtime refusal, stale plans and interrupted migration. `just --no-dotenv local-workbench-v1-phase-2-check` passed including the new tests and 21 offscreen Qt rows. This is not a live reboot PASS.

Read-only live `manual/workbench-runtime-receipt-recovery.ts --plan` returned v1 receipt SHA-256 `397171494262f312fd9249d32cc5d207e94ff26581b0a3f4b4549f91cbcd02af`, proposed v2 SHA-256 `905600091b9483f8404c8fdfd2951f04d3726ffe3b81c1e7baa89bad2109fe33`, plan digest `1c060606ea1fcb55624b30148e44e8cb76124012e81d9c36029d69b036b48192`, and exactly the two `/run` drift paths above. The operator typed `y` in a visible TTY for that exact plan (`~/.local/state/omarchestra/manual-gates/workbench-runtime-recovery-tty-20260925T074141Z.log`, private mode 0600). The old v1 receipt was preserved byte-for-byte at `manual-gates/workbench-ownership-v1-397171494262f312fd9249d32cc5d207e94ff26581b0a3f4b4549f91cbcd02af.json`; the new receipt is version 2 with SHA-256 `905600091b9483f8404c8fdfd2951f04d3726ffe3b81c1e7baa89bad2109fe33`. Readback verified every persistent recorded inode still matches. `systemctl --user reset-failed` then `start` returned a stable background owner at epoch 9, PID 15608, with 1 Project and 1 Goal. The installed bar's exact desktop command `gtk-launch omarchestra-workbench` returned `opened`, followed by a delayed `presentation: open` from the same service PID. No Pi process was created. The operator's physical bar click after this recovery and a second physical login reboot are separate human validations. `pi list` still shows the global observer, but there is currently **no Pi process**; no footer/Observed Pi Session can appear until a user starts a visible Pi normally. No automatic Pi/model session restoration is promised.

## 2026-09-26 — repeated login failure: persistent device-number drift

Read-only inspection of the enabled `omarchestra-workbench-owner.service` found it failed three times at 09:06:22–09:06:28 with `identity_drift`, then hit systemd `start-limit-hit`. The exact v2 receipt still hashes to `905600091b9483f8404c8fdfd2951f04d3726ffe3b81c1e7baa89bad2109fe33`, the value recorded in the prior migration closeout. `/home` remains the Btrfs `@home` mount. Every recorded `/home` descendant and owned database has the same recorded inode, uid and kind; only `st_dev` changed from `58` to `53`. `/` remains unchanged. The private runtime root is mode 0700 and empty; read-only `PRAGMA integrity_check` returned `ok` for all three SQLite files. This is not a missing/corrupt database, stale socket, concurrent Owner, or Companion install failure.

The bar plugin is still enabled in Omarchy's read-only plugin list and `shell.json` contains the receipt-owned bar widget. The installed desktop launcher executes the development-checkout `manual/workbench-service-toggle.ts`; before this correction it refused `ActiveState=failed`, so the already-enabled launcher could not clear the start-limit after its three retries. It now permits `reset-failed` only for this exact enabled, receipt-owned unit after verifying no socket exists, then asks systemd for one supervised start. Active, activating, foreign, disabled, stale-daemon-reload, or socket-present cases remain fail-closed. Focused regression tests pass.

`pi list` still registers `~/.local/share/omarchestra/pi-observer.ts`; the file is mode 0600, and a no-session explicit-extension preflight exits 0. This proves registration and import/preflight, not that an already-running Pi TUI completed its `session_start` handshake. The Pi status slot is set only after the Owner returns a registration challenge. With the Owner socket absent, the extension cannot register and its status is intentionally unset; the lack of a footer is downstream of the failed Owner, not evidence that the Pi installation entry was removed. No Pi session was started or reloaded for this investigation.

The read-only v2→v3 migration plan has receipt hash `905600091b9483f8404c8fdfd2951f04d3726ffe3b81c1e7baa89bad2109fe33`, proposed receipt hash `df9c4effef847ecf04baf8d6dfc0eac8ba701d5e60d96a3061c1d0b8077c0989`, and exact plan digest `663feebcde84860ff6c35bf0d67cd626f15bd7bedd035bf30d77e1bc6457c63b`. It reports ten `/home` paths where the legacy `dev` field differs, without exposing those device numbers, and proposes the current Btrfs mount tuple (`/dev/mapper/root`, root `/@` and subvolume ID 256 for `/`, root `/@home` and ID 257 for `/home`) plus unchanged inodes. The plan command made no writes. Targeted tests cover v1/v2 forward-only recovery, legacy-field drift omitted from v3, unchanged history, interrupted migration evidence, same-lifetime mount replacement, and bar recovery from start-limit-hit. Live migration, `systemctl reset-failed/start`, dock opening, and Pi handshake remain intentionally unperformed: the helper requires an operator-visible TTY confirmation for this exact plan. No global extension, QML asset, `shell.json`, Team Goal, Project or Pi file was changed.
