# Same-button confirmation correction (2026-09-25)

Status: **0.11.0 installed with verified bar-command → native dock open; live two-press Adoption remains untested because no Pi is running.**

The operator rejected the 0.10.0 inline `Review exact action` box shown after `Adopt as implementer`. The approved replacement is two presses of **that same action button**, without a separate confirmation surface, hover tooltip, desktop notification or focus jump. The first press only arms the committed choice; it relabels that button `Confirm: …`. A second press emits the captured exact intent only if the choice, Goal, Role, current session, generation, epoch and relevant projection facts still match. Cursor-only heartbeats do not disarm it. Escape, navigation, an unrelated action, disconnection, a relevant change or the 30-second timeout disarms it. A press after invalidation only arms again. Proposal authorization and same-process Pi ACK remain distinct runner-gated steps; this is **not** a live Adoption or dispatch claim.

Red gate: the newly written offscreen QML same-button tests failed against the original in-dock box (`/tmp/omarchestra-two-press-red.log`). Green gates: the offscreen QML journey passed 21/21, the composed fake-owner/QML scenarios and direct QML-method regressions passed, and the full disposable `packages/local-workbench-v1/scripts/phase-2-gate.sh` exited 0 **again** after archived-release coverage, removal of hover tooltips and new-Projection-Session disarming (`/tmp/omarchestra-two-press-phase2-final.log`). None of these gates contacts the live Companion, Pi or owner. The installed 0.10.0 tree was copied byte-for-byte to `packages/local-workbench-v1/companion/retained/0.10.0/` before changing the 0.11.0 source.

**Live installation blocker (do not claim UI acceptance):** Read-only inspection before the attempt approved an exact 0.10.0→0.11.0 update with 15 assets, 1 bar widget, no Pi and owner epoch 9. The visible setup TTY displayed that plan; the setup then exited **1** (`~/.local/state/omarchestra/manual-gates/workbench-same-button-setup-20260925.log`). The installer did not capture its failure text to the private log. Its private plan directory is `manual-gates/companion-setup-53804-N1bl4F/` and has no installation result. After the attempt, all 15 installed asset **bytes** exactly match archived 0.10.0, the manifest is still 0.10.0, the bar still has one placement, and the 0.10.0 receipt and `shell.json` were unchanged; **all 15 installed asset inodes differ from the receipt**. A subsequent read-only `CompanionInstallation.inspect` correctly fails `foreign_installation` at `AgentConsole.qml`; no retry, receipt rewrite or deletion is permitted without a new exact-evidence recovery contract and operator authorization. The background owner continues to report 1 Project and 1 Goal, with no Pi; the loaded shell reports 0.10.0. The error was not captured, so do not infer which post-write step originally failed. One established contributing defect was that the real filesystem's `writeBytesAtomic` replaces an existing inode, while the fake filesystem falsely preserved it; restore-by-rewrite cannot re-establish the receipt-pinned original inode after a failed update. A red fake bar rollback gate reproduced this mismatch (`/tmp/omarchestra-atomic-inode-red.log`). Source now models atomic writes with new fake inodes, stages **all** old owned assets before writing, and restores the exact staged inode on failure; a real no-follow filesystem regression verifies the inode change. The focused installer regressions and the full disposable Phase-2 gate now pass again (`/tmp/omarchestra-two-press-atomic-gate.log`). This protects future attempted updates, **not** the already-drifted live receipt. At this historical checkpoint the 0.11.0 UX remained **source/offscreen-only** and the user's old UI was still installed. The repair had to use a separate exact-evidence v0.10.0 receipt-rebinding plan with external backup and the operator's own TTY consent. The initial failure text was not captured, so its initiating post-write cause remains unknown; do not infer it from the rollback bug.

## Exact recovery and installed verification

- `manual/workbench-companion-receipt-recovery.ts --plan` verifies the failed plan digest, receipt SHA-256 `bb81331b750bb705c9e77a3ba7f9e9f7c729376df6864e4317af81fa4b47c6ad`, **original full tree digest** reconstructed from the receipt's pinned inodes, the private setup failure chronology, the byte-identical archived 0.10.0 release, exact unchanged `shell.json`, and all 15 currently owned file identities/bytes/modes. It refuses unrelated or changed resources. The bounded disposable recovery regressions and full Phase-2 gate passed (`/tmp/omarchestra-samebutton-rebind-final-gate.log`; 21 offscreen QML rows).
- In a visible TTY the operator approved this **separate receipt-only recovery**. The private `workbench-companion-rebind-20260925.tty.log` and `.result` show exit 0. Its original v0.10.0 receipt was preserved byte-for-byte at `manual-gates/companion-pre-rebind-bb81331b750bb705c9e77a3ba7f9e9f7c729376df6864e4317af81fa4b47c6ad.json` (0600). Readback proved every asset byte and its new receipt-pinned inode; `CompanionInstallation.inspect` then accepted a new, still-unexecuted 0.11.0 plan. No Pi/Owner restart, asset update or bar edit occurred in the recovery.
- The operator **separately** approved the exact 0.10.0→0.11.0 installation in a second visible TTY. The private `workbench-same-button-update-retry-20260925.tty.log` and `.result` show exit 0; the receipt is 0.11.0 (SHA-256 `0a2efb7b0467b6774be5a6c51d422f8665a82aae6ecb7e9d7e13d140539c786b`), with previous release 0.10.0. All 15 installed assets match receipt bytes and inode identities. Current `shell.json` is **byte-for-byte identical** to the backed-up original v0.10.0 postimage; it retains its one right-hand Omarchestra bar widget and every unrelated widget. An independent read-only installer inspect passes.
- `omarchy restart shell`, then a background service restart while presentation was hidden and no Pi was running, loaded Companion 0.11.0 with Owner epoch 10, 1 Project and 1 Goal. The exact installed bar command `gtk-launch omarchestra-workbench` returned `opened`; delayed Owner status remained `open` with the same service PID 80647. The loaded native `presentationContract` reports 0.11.0; the installed QML contains no `Review exact action`, `workbench-inline-confirmation`, separate Confirm button or hover tooltip. **This is command-path/native open plus source/offscreen two-press evidence, not a physical Adoption-button click or a live Pi Adoption.** No Pi process was launched; Assignment and check execution remain outside Phase 2.

## Version label follow-up (2026-09-26)

The loaded Companion release version is now shown in small text beside the Runner connection state. This is an additive 0.12.0 release; the installed 0.11.0 assets are retained byte-for-byte under `companion/retained/0.11.0/`. Offscreen rendering and fake receipt-backed 0.11.0→0.12.0 update/rollback tests pass. **0.12.0 has not been installed or shell-reloaded by this change.**
