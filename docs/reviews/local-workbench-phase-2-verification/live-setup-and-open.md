# Human live setup and native open — partial walkthrough

Status (2026-09-24): **Companion 0.9.0 installed with a live bar placement and in-dock Close method; command-path hide/reopen and global Pi observation verified, physical bar/Close clicks and exact implementer Adoption still pending.** The 0.8.0 history below remains a chronological account, not the current installation. These actions were operator-directed on the real Omarchy desktop. No Assignment or check was executed. The exact Pi target has not been confirmed for implementer Adoption.

- Read-only `CompanionInstallation.inspect({ operation: 'update', release: WORKBENCH_PREVIEW_RELEASE })` on Omarchy 4.0.4-1 and installed 0.7.0 passed capability/ownership checks. First TTY-authorized attempt stopped *before mutation* with `stale_precondition`: `shell.json` had gained only an unrelated `omarchy.tailscale` widget in `bar.layout.right` since the 0.7.0 receipt. No foreign plugin state was overwritten.
- Added an update-only, inspect-and-execute-validated bar rebase: the receipt's original plugin delta and exact owned assets must validate, current config may differ only in the unrelated bar subtree, formatting must match supported canonical JSON, and the next receipt carries forward the new bar as its uninstall preimage. All other drift, changed plugin identity and stale plans still fail closed. Fake tests cover allowed drift and negative cases; the existing installer tests pass. The operator typed `y` in a real TTY for the new plan. 0.8.0 was installed and the receipt/assets verified; the user shell was restarted and the loaded Companion contract negotiated.
- Initial native `start`/`open` returned an open acknowledgement but the view disappeared after the next tick. Real shell calls proved an empty `takeIntent` response is valid; the adapter incorrectly treated that as unavailable. After a focused fix and test, owner-side error reporting exposed a second issue: direct installed-shell `applyProjection` delivers encoded JSON, while QML only accepted an object. Both failures became regressions in the native-owner and offscreen-QML tests. The full disposable Phase-2 gate again exited 0, now with 19 offscreen Qt rows.
- Source QML bytes had changed. With the owner stopped, the operator typed `y` for a second exact 0.8.0 same-version update in a visible TTY. The prior receipt was privately archived before update. The updated receipt matches the exact current release assets; the shell was restarted and a probe summon loaded the panel before negotiating its identity, version, protocol, capabilities, destinations and generation. The actual foreground owner resumed at runner epoch 4 in private roots under `~/.local/state/omarchestra/workbench-phase2-live` and `$XDG_RUNTIME_DIR/omarchestra-workbench-phase2-live`. After `open`, a four-second delayed `status` still reported `presentation: open`; `hyprctl layers -j` reported `omarchestra-local-workbench` on HDMI-A-1 (420×1054). Earlier open acknowledgements alone were **not** counted as a pass.
- A separate empty Git repository `/tmp/omarchestra-live-demo.XK3aAy` was created and accepted by the read-only Project inspector, and was later registered via the native workbench. The operator entered this path and clicked Inspect; initially Confirm and register did not enable, then the dock disappeared on a second click. `coredumpctl` shows **no contemporaneous process crash**; shell and foreground owner remained alive. The owner log records `workbench presentation tick unavailable: intents require a non-gap authoritative connection`, after which the host deliberately hid the dock. Zero Projects/Goals were committed. A red fake native test reproduced the same disappearance when shell intent readback lasted longer than the 2-second freshness bound. The owner now refreshes the authoritative source immediately before a queued click; a changed revision/selection rejects the unsent click locally as stale rather than forwarding it, while unchanged fresh source permits it. Slow-readback and changed-context regressions plus the full fake/offscreen Phase-2 gate pass. Owner restarted at epoch 5 and the dock is visible again on DP-1. The operator reported the Inspect flow then worked; read-only `status` confirmed the same open session, zero owner tick errors, `projects: 1`, `goals: 0`, `revision: 1`. This confirms a Project was registered in the native flow, but a separate click-by-click receipt was not captured. Goal creation, Pi observation and exact confirmed Adoption remain human actions. No Pi process, Project checkout, or real Assignment was touched by setup/open. Historical 0.6/0.7 release assets remain unchanged.

2026-09-24 continuation: An ordinary Pi was started in a separate visible foot terminal (`org.omarchestra.demo-implementer`, demo Git Project cwd) with an explicitly loaded observer wrapper pointing at the owner-only live bridge socket. The compositor reported the window mapped; `ss` correlated the new Pi process with an established connection to that socket. The operator's screenshot shows the actual Pi TUI footer `Unassigned · observed` alongside one `observed · available · running` row on Add agent. **No role has been granted and no Adoption has occurred.** This is a manually started ordinary terminal, not a product-managed terminal spawn or Boomux PTY. The operator explicitly rejected describing it as an existing *implementer* window and reported there is no in-product Spawn/Create action; that is correct for Phase 2. The previous `π - Work` Pi process was no longer running when checked, so it must not be adopted as reviewer by stale title or PID.

The operator explicitly authorized a **persistent global Pi observer registration**. A private, owner-only Pi extension entry at `~/.local/share/omarchestra/pi-observer.ts` points at the current live owner socket and imports the development-checkout bridge. `pi --no-extensions --extension <entry> --help` passed a no-session load preflight, and `pi install <entry>` registered the local source in Pi's user settings; `pi list` confirmed it. Existing Pi processes must reload to pick it up; future ordinary Pi processes discover it automatically while the development checkout and owner socket remain available. This is an operator-directed development installation, not a standalone packaged observer or proof of an auto-started runner. The installed Omarchy Companion 0.8.0 was already persistent and enabled via `shell.json` before this request. The native UI currently has **no Close control or persistent bar opener**: owner `hide`/`open` one-shot commands exist, but a Boomux-like graphical toggle and owner autostart have not passed live acceptance. A live owner-only `hide`→`status`→`open`→`status` round trip returned `hidden` then `opened` with a fresh Projection Session, unchanged 1 Project/1 Goal/revision 6, and the ordinary Pi process survived. The operator then pointed out that the command-only round trip was not an operable UI. An explicitly requested user-local desktop launcher (`~/.local/share/applications/omarchestra-workbench.desktop` → private `~/.local/share/omarchestra/toggle-workbench`) now supplies a graphical application-menu toggle while the foreground owner survives. It queries the owner and sends exactly `hide` or `open`; unavailable/unknown status fails without starting another owner. A live `gtk-launch omarchestra-workbench` twice returned `open → hidden → open`, with the same owner and live Pi PID. This **is not a dock Close button, bar widget, managed Pi terminal launcher, or autostart service**; until a versioned Companion and shell integration supply those, Boomux-like bar operation is not accepted. No Companion installer was re-run and no Omarchy shell/configuration files were changed in this continuation.

A later operator screenshot shows a `reviewer · managed · connected` card and no observed sessions, but no independently captured same-process binding transcript was collected here; do not retrofit the earlier evidence with a claimed implementer Adoption. Following the operator's explicit request for an in-dock Close button and Boomux-style bar opener, source now has a presentation-only `hide_workbench` request: QML → native owner disposes only the exact current view, with no runner command or database event. Native owner regression and offscreen Qt click regression pass (20 Qt rows); the full disposable Phase-2 gate passes. **This corrected QML is not installed in the live 0.8.0 plugin**; an exact-plan TTY-authorized update is required before claiming a live Close button. A bar widget, its layout installation and executable opener are not implemented; the user-local app launcher remains the only live graphical toggle. The owner and installed Companion were not restarted or silently modified by the source-only repair. The operator subsequently approved a native bar button and dock Close update. The read-only [bar lifecycle assessment](../../../spikes/workbench-bar-lifecycle/README.md) found that the supported Omarchy `enable`/`put` paths cannot promote the existing receipt-owned `plugins[]` panel entry to a same-ID `bar.layout` widget, while the current installer's update/rollback/uninstall assumes exactly that panel entry. This blocks an exact-plan live update until a tested owned migration exists; the authorization did not waive receipt or rollback safety. No live setup, shell.json write, plugin update, or Pi/owner restart was performed as part of this assessment.

2026-09-24 bar/Close continuation: [The owned bar migration spike](../../../spikes/workbench-bar-lifecycle/README.md) documents the upgrade and tests. After all disposable installer/native/QML gates passed, `omarchy plugin validate` passed; the operator typed `y` in a real TTY for the **exact 0.9.0** plan (private log `workbench-bar-setup-tty-20260924T144530Z.log`, receipt privately archived beforehand). The 0.9.0 receipt's `shell.json` postimage matches the installed config: exactly one Workbench bar entry in `right`, zero old `plugins[]` entries, 0.8.0 preserved as previous release. After `omarchy restart shell`, loaded Companion capabilities and presentation contract report 0.9.0 and `listPlugins` reports `panel` plus `bar-widget`; no post-restart widget-load warning. A transient pre-restart hot reload did log a filename-case warning, so hot-reload was not counted as acceptance. `/usr/share/omarchy/shell/plugins/bar/Bar.qml` was not modified.

The foreground owner was gone on post-install check (reason unknown); its exact, unbound socket inodes were verified under an exclusive owner lock and **moved, not deleted**, to the private runtime quarantine `orphaned-owner-sockets-dL0Hxt`. A new owner opened the same history at epoch 6 with 1 Project/1 Goal. `open` remained stable, live panel `requestHide` returned `hidden`, and `gtk-launch omarchestra-workbench` (the exact fixed widget command) reopened with a new Projection Session; the original Pi PID 483736 remained running. This is live owner/QML method and command-path evidence, **not a physical button click**. A new visible ordinary Pi (`org.omarchestra.global-observer-pi`, PID 571497) started with globally registered extensions (no explicit observer flag), connected to the owner bridge and remains Unassigned; its window title is not role authority. A local screenshot of the HDMI-A-1 bar showed the circular widget icon immediately left of the app grid, and the shell reports it loaded; this still does not prove a click. Real physical bar → dock Close → bar clicks, footer/observed-row confirmation for this new Pi, and a user-confirmed exact implementer Adoption remain to be recorded. Owner is currently epoch 6 in a visible foreground terminal; no service/autostart is promised.

Operator follow-up: the operator confirmed that physical bar open/Close works, then stopped foreground owner epoch 6 with Ctrl+C (`closed`), so the backend is **not currently running** and the bar button cannot reopen a dock until an owner is explicitly started. The operator reported two unfamiliar windows: the foreground owner and the agent-started Pi named `Omarchestra implementer candidate`. The agent sent SIGINT only to the exact latter Pi PID 571497, verified its exit, and closed only its foot window PID 571458. Neither was an Adoption. The earlier demo Pi PID 483736 also exited around this time, but was not signalled by that cleanup; no live Pi is now visible in those test windows. No owner/service was restarted. In response to the operator's rejection of a required terminal, `manual/omarchestra-workbench-owner.service` and its bounded source test were added; `systemd-analyze --user verify` and the disposable Phase-2 gate passed. The operator subsequently authorized enabling this exact service without starting it and requested bar-initiated on-demand startup. An exact unit-identity-checked one-shot launcher (`manual/workbench-service-toggle.ts`) now requests only this user service through systemd if the owner socket is absent, waits for real owner readiness and then sends `open`; negative fake tests reject disabled/foreign/stale/active-but-unreachable services and unconfirmed results. The disposable Phase-2 gate passes. The previously installed private desktop/bar helper was archived under `manual-gates/workbench-toggle-pre-service-20260924T154013Z` and atomically replaced with the checked launcher wrapper. `systemctl --user enable <exact checked-out unit>` created only its own unit and `graphical-session.target.wants` links. Readback reports `enabled`, `inactive`, `NeedDaemonReload=no`, canonical unit source inside this checkout; **no owner or Pi was started during enablement**. The operator subsequently confirmed the **real bar click and in-dock Close button work**. Read-only follow-up after their clicks: systemd reports this exact unit `enabled`/`active`/`running`, MainPID 629637 and `NeedDaemonReload=no`; the owner reports epoch 7, 1 Project, 1 Goal and an open Projection Session. No Pi process was created by service startup. This is live local presentation acceptance, **not** Pi Adoption, Assignment delivery, check execution, or crash-restart validation. Inline-confirmation follow-up: in response to the operator's screenshot and explicit no-pop-up request, source Companion 0.10.0 replaces the sole action `Dialog` with an in-dock exact-target review. A cursor-only heartbeat preserves the review; expiry or relevant revision/choice change leaves an inline warning and disables confirmation; no Adoption or dispatch is inferred. The old installed 0.9.0 QML was archived byte-for-byte under `packages/local-workbench-v1/companion/retained/0.9.0/` after checking its receipt. The full fake/offscreen gate passed, including 21 Qt rows and fake bar-to-bar rollback/uninstall. Read-only live inspection approved a 0.9.0→0.10.0 exact plan, and a graphical setup TTY displayed `Install 0.10.0? [y/N]` in `manual-gates/workbench-inline-setup-tty-20260924T162442Z.log`. No `y` was entered during the initial wait, so 0.9.0 remained installed **at that checkpoint**. The operator subsequently typed `y`; the same private log records `COMMAND_EXIT_CODE=0` and installation result 0.10.0. Read-only receipt check verified previous release 0.9.0, one exact bar placement, zero old panel entries, and `shell.json` byte equality. A post-install click failed with an unwanted transient **desktop notification** `owner_action_not_confirmed` (operator screenshot): the foreground service was still holding 0.9.0 runtime modules while the loaded Companion had switched to 0.10.0. The `notify-send` failure path was removed from `manual/workbench-service-toggle.ts`, and a disposable source test now rejects its reintroduction. After `omarchy restart shell`, the unchanged owner still refused 0.10.0; an explicit `systemctl --user restart omarchestra-workbench-owner.service` while presentation was hidden and no Pi was running closed epoch 7 cleanly and started epoch 8, with the same 1 Project/1 Goal. The loaded shell reports 0.10.0; a real one-shot `open` returned `opened` and a delayed status remained `open`. No Pi process was started. The full disposable gate passes (21 offscreen Qt rows). The owner is now a background service with the dock open; a physical inline Adoption review still awaits a current ordinary Pi, and no Adoption is claimed. Private TTY/evidence logs remain under `~/.local/state/omarchestra/manual-gates/` (mode 0700, files 0600), not in the repository. Do not describe an empty live dock as complete management acceptance.
