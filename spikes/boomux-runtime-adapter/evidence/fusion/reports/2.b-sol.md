Created:

- `spikes/boomux-runtime-adapter/README.md`
- `spikes/boomux-runtime-adapter/evidence/cli-source-truth.txt`
- `spikes/boomux-runtime-adapter/evidence/manual-observations.md`

Evidence captures Boomux 1.8.0, the complete `boomux.cli/v1` capabilities envelope, and relevant help output. No daemon contact, mutation, or GUI command ran.

Documented the command/envelope contract, weak non-JSON operations, safety policy, exact-Run race, cleanup rules, provisional conclusion, design implications, and pending human gate.

Validation:

- `git diff --check` passed.
- Evidence contains version, schema, protocol 49, capabilities, and `open` help.
- Forbidden live-command audit found none.
- Manual record remains explicitly `PENDING`.

Handoff: adapter implementation should follow the exact commands and compatibility checks in `README.md`. Final integration must expand the disposition table to name every implemented file and replace pending automated evidence without changing the manual gate status.