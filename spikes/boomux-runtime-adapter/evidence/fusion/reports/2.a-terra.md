Implemented the adapter and manual harness. No live Boomux mutation or GUI command ran.

Changes:

- `spikes/boomux-runtime-adapter/lib/adapter.mjs`
  - Six-method port, capability and running-daemon checks, opaque refs, lifecycle mapping, event filtering, exact-ID cleanup.
  - Empty global Workspace then three explicitly named role Shells.
  - JSON readback reconciles global Workspace, owner Workspace, Shell, and Run IDs.
  - Weak command failures become `unknown_outcome` and block cleanup.
- `lib/commands.mjs`, `envelopes.mjs`, `errors.mjs`, `executor.mjs`, `receipt.mjs`
  - Exact argv builders, strict `boomux.cli/v1` parsing, typed errors, direct `spawn(..., { shell: false })`, private receipt stores.
- `manual.mjs`
  - Read-only preflight, explicit mutation authorization, explicit GUI authorization, role operations, private evidence and receipt paths.
- `probe-process.mjs`
  - Emits role, PID, Shell ID, and `BOOMUX_RUN_ID`, then remains alive.
- `.gitignore`
  - Ignores `evidence/local/`.

Validation:

- `node --check spikes/boomux-runtime-adapter/lib/*.mjs`
- `node --check spikes/boomux-runtime-adapter/manual.mjs`
- `node --check spikes/boomux-runtime-adapter/probe-process.mjs`
- Fake-executor capability and create/idempotency flow passed.
- `git diff --check` passed.
- Source scan found no hidden attachment command, private Boomux state path, daemon socket path, shell execution API, or forbidden desktop command.

Handoff for task 3.a:

1. Import `BoomuxRuntimeAdapter`, `MemoryReceiptStore`, `REQUIRED_*`, command builders, and envelope/error helpers.
2. Fixtures must model qualified IDs in `node.snapshot.local_snapshot` and scalar IDs from `shell.inspect`.
3. `create()` performs capability checks and captures an event baseline before the weak Workspace creation command.
4. Test `open` argv includes the documented exact Shell, global Workspace, title, and `--takeover`, but fake executors must prevent any real GUI command.
5. Keep the manual observation gate pending.