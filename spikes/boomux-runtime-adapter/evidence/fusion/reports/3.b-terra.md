Validated adapter and manual harness. `node --test spikes/boomux-runtime-adapter/test/*.test.mjs`: **58 passed, 0 failed**. All syntax checks and `git diff --check` passed without live Boomux commands.

Fixed reconciliation, NUL argv rejection, baseline validation, Run event filtering, cursor reseeding, and ambiguous-mutation retry blocking. Added regression tests in `test/adapter.test.mjs`.

Handoff report: `/tmp/fusion-harness-FQG0OZ/collaborate/reports/3.b-terra.md`. Human GUI/detach/reconnect validation remains pending.