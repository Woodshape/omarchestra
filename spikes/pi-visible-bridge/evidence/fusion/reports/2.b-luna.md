Implemented task 2.b.

### Changes

- `spikes/pi-visible-bridge/extension.ts`
  - TUI-only startup.
  - Starts bridge client during `session_start`.
  - Idempotent cleanup during `session_shutdown`.
  - Handshake and state snapshot support.
  - Idle assignment injection through `pi.sendUserMessage()`.
  - Assignment deduplication and acknowledgements.
  - Lifecycle, message, tool, and UI attention telemetry.
  - Interactive-only human takeover detection.
  - `/bridge-attention-probe` confirmation command.
  - No process spawning or SDK session creation.

- `spikes/pi-visible-bridge/lib/client.mjs`
  - Unix-socket NDJSON client.
  - Reconnect with bounded exponential backoff.
  - Re-handshake after reconnect.
  - Safe idempotent close.

- `spikes/pi-visible-bridge/test/extension.test.mjs`
  - State projection and settlement.
  - Interactive versus extension source classification.
  - NDJSON serialization.
  - Handshake, assignment delivery, and reconnect behavior.

### Validation

Passed:

```text
node --check spikes/pi-visible-bridge/lib/client.mjs
node --check spikes/pi-visible-bridge/test/extension.test.mjs
node --test spikes/pi-visible-bridge/test/extension.test.mjs
git diff --check
```

Result: 4 tests passed.

Manual visible-TUI validation remains outstanding and must be performed before claiming visible-TUI support.