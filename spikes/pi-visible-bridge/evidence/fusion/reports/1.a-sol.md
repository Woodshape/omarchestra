Implemented task 1.a.

Changes:
- `spikes/pi-visible-bridge/lib/protocol.mjs`
  - Strict versioned NDJSON protocol and framing
  - Message constructors and validation
  - Size and telemetry limits
  - Fragmented UTF-8 frame support
- `spikes/pi-visible-bridge/lib/state.mjs`
  - Pure connection, assignment, takeover, event, and snapshot state helpers
  - Busy, duplicate, accepted, and invalid acknowledgement support
  - Stable sequenced event IDs
  - Bounded telemetry sanitization

Validation passed:
- Node syntax checks
- Fragmented and combined NDJSON frames
- Reconnect state transition
- Assignment acceptance, busy handling, and deduplication
- Event sequencing
- Interactive versus extension input classification
- Strict unknown-field rejection
- `git diff --check`

Handoff: runner and extension should import the message constructors, `NdjsonDecoder`, and state helpers directly. Call `markConnected()` after `hello_ack`, then emit its returned event type and send `createStateSnapshot()`.