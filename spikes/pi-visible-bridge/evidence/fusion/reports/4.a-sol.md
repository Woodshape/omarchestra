Completed task 4.a.

Changes:
- Added `spikes/pi-visible-bridge/README.md` with the full spike record and manual verification procedure.
- Updated `spikes/pi-visible-bridge/evidence/automated.txt`.
- Fixed integration defects:
  - Attention now uses Pi `ui_prompt_start`/`ui_prompt_end` hooks.
  - `session_started` is emitted after connection instead of being lost.
  - Settled assignments are not restarted or attached to later unrelated turns.
  - Thinking and raw tool payloads are excluded from telemetry.
  - Socket close attempts a graceful flush.

Validation:
- Pi `0.84.4`
- Node `v26.8.1`
- 11 tests passed
- Syntax checks passed for all JavaScript modules and tests
- `git diff --check` passed
- Changes remain under `spikes/pi-visible-bridge/`

Handoff: automated feasibility passed with constraints. Visible-TUI support remains pending the documented human gate and is not claimed.