Changes:
- Added `spikes/pi-visible-bridge/runner.mjs`
  - Unix-domain socket runner with runner-owned stale socket cleanup.
  - Strict hello-first handshake and optional deterministic assignment delivery.
  - Structured JSONL stdout capture for lifecycle, messages, acknowledgements, and protocol errors.
  - Graceful `SIGINT`/`SIGTERM` foreground shutdown.
  - No Pi, SDK, child-process, or PTY imports.
- Added `spikes/pi-visible-bridge/test/runner.test.mjs`
  - Fragmented framing, malformed JSON, hello-first enforcement, assignment acknowledgements, and listener replacement.

Validation:
```text
node --check spikes/pi-visible-bridge/runner.mjs
node --check spikes/pi-visible-bridge/test/runner.test.mjs
node --test spikes/pi-visible-bridge/test/runner.test.mjs
```

Result: 5 passing tests. `git diff --check` passed.

Handoff:
- Extension client connects to `RunnerStub.socketPath`.
- On `hello`, runner sends `hello_ack`, then optional `assignment`.
- Runner accepts bridge-originated `assignment_ack`, `event`, and `state_snapshot`, exposing captures through `records` and acknowledgements through `acknowledgements`.
- Manual command:
```bash
node spikes/pi-visible-bridge/runner.mjs --socket /tmp/omarchestra-bridge.sock --assignment-id assignment-1 --prompt "Inspect this spike"
```