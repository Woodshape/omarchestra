Changes:
- Added `spikes/pi-visible-bridge/test/integration.test.mjs`.
  - Real Unix-socket runner/client handshake.
  - One initial assignment delivery and accepted acknowledgement.
  - Runner restart with busy acknowledgement, fresh handshake, reconnect snapshot, and `bridge_reconnected`.
  - Second restart with duplicate acknowledgement.
  - Ordered event sequences and continued post-reconnect event capture.
  - Source audit rejects SDK session APIs, `child_process`, Pi spawning, and PTY injection.
- Added `spikes/pi-visible-bridge/evidence/automated.txt` with complete test output.

Validation:
```bash
node --check spikes/pi-visible-bridge/runner.mjs
node --check spikes/pi-visible-bridge/lib/client.mjs
node --check spikes/pi-visible-bridge/test/runner.test.mjs
node --check spikes/pi-visible-bridge/test/extension.test.mjs
node --check spikes/pi-visible-bridge/test/integration.test.mjs
node --test spikes/pi-visible-bridge/test/*.test.mjs
git diff --check
```

Result: 11 tests passed. Exact output is in `spikes/pi-visible-bridge/evidence/automated.txt`.

Handoff:
- Sol can complete the spike record using the evidence file.
- Automated coverage does not establish visible-TUI behavior. Manual validation remains required before any support conclusion.